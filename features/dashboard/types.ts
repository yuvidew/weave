// Which slice of a Run each DashboardListPanel is showing — decides whether
// a row's right-hand side renders a RunStatusBadge or the run's result text.
export type DashboardListVariant = "attention" | "running" | "completed" | "scheduled"
